# DEPRECATED / HISTORICAL — New Session Kickoff

This kickoff predates V0.1 Official Runtime Entry canonicalization. Do not execute its former `ai-control.cmd`, bsk, daemon, Bridge, session, or browser-internal startup instructions as the current production path.

For any current Worker or replacement AI:

1. Read `runtime/WEAK_WORKER_START_HERE.md`.
2. Use `E:\WB\tools\ai-production-control\runtime\run.cmd` as the only OFFICIAL Runtime Entry.
3. Treat `ai-control.cmd` / `scripts/ai_control.py` as COMPATIBILITY / LEGACY only.
4. Let Runtime own Bridge/daemon/session/marker/click details; do not bypass the Runtime entry.

Historical implementation notes remain available in Git history but are not current startup instructions.
