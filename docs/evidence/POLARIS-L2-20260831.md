
# POLARIS-L2-20260831 — 北极星 L2 闭环验证（接线后）
- 日期：2026-08-31 11:10（北京时间）
- 分支：v1.1-blackbox，HEAD = 5df9ea8（接线刀）后
- 性质：**L2 模拟实测**（relay_autopilot 沙箱全链 + 真实账本），非 L3 真实弱模型/R 审查（见 E1）

## 流程（submit → claim → work → report → R 门 → review → PASS → WRAPPED）
1. `submit --goal-file polaris-goal.json --mode mock`：**调度准入三闸全部通过**（§59 ALLOWED weak ETC=0.05、§55 SUFFICIENT、§34 lease acquired gen=1），事件 EV-AUTO-20260831110737-57945 入沙箱 inbox。
2. `drive --watch --mock-review PASS --max-reworks 8 --max-wait 30`：3 轮收敛，新增 run RUN-AUTO-20260831110737-57945 全链 **WRAPPED**（queue 3 runs 全 WRAPPED：历史 2 + 新增 1）。
3. 账本（E:\WB\state\...\autopilot-actions.ndjson）：submit→claim→work_start→work_done→review_enter(WAITING_REVIEW, R gate concurrency=1)→review_start→review_pass→drive_end 全链留痕。
4. lease 证据：state/controller_lease.json gen=1 holder=relay_autopilot（§34 Controller 级代），drive 执行前校验通过。

## 接线验证对照（审计 §4.4 三项）
| 节 | 接线点 | 本验证证据 |
|---|---|---|
| §59 | cost_router.do_route 挂 submit 准入 | admission_checks → ALLOWED（weak/ETC=0.05）；SAFE_HALT 拒绝路径有单测（test_relay_autopilot_wiring_offline） |
| §55 | context_sufficiency.route 挂 submit 准入 | admission_checks → SUFFICIENT；BLOCKED/HUMAN_AUTHORIZATION 拒绝路径单测 |
| §34 | controller_lease 挂 drive + admission | drive 前 check（gen=1 OK）；老代/超时拒绝单测 7 用例 |

## 测试基线（接线后全量）
- 全量回归（Windows 原生 PowerShell + Python312）：**568 tests OK**（553 基线 + 15 新增：wiring 8 + lease 7），零失败
- 权威矩阵 36/36（B1/B3/B2/接线均零改动 `on_b1_core.py`）
- doctor：DRIFT_FREE

## 未做（L3，真实边界）
- 真实弱模型会话 + 真实 R 审查（browsers connected=0，E1）→ 丢审核链接裁决，等业主/主脑

## 相关文件
- 接线代码：scripts/relay_autopilot.py（admission_checks + drive lease 门）
- 新模块：runtime/controller_lease.py（§34）
- 测试：runtime/test_relay_autopilot_wiring_offline.py（8）、runtime/test_controller_lease_offline.py（7）
- 文档：docs/ops/autopilot-README.md（新增"调度准入三闸门"节）