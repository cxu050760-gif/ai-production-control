> 
> **N1 修订注（2026-08-31）**：本报告中所有 "104 条目" 为 R2 审核时点（commit a35bced）实测值；D3 后 registry 扩充至 **107 条目**（实测：python312 docs/ops/registry-validate.py → sections 15/15 | entries 107, EXIT=0）。业主验收请以 107 为准。
> 
# REVIEW-R2-2026-08-30-QA — R2 能力注册表 + 资产接入独立审核（QA 会签）

- 审核者：software-qa-engineer（严过关，QA 会签之一）
- 日期：2026-08-30
- 分支：v1.1-blackbox
- 审核对象（R2 交付，实现者 software-engineer-r2）：
  - `config/capability-registry.json`（新建 1447 行，schema_version 1，顶层 15 节 + role_bindings，104 条目）
  - `docs/ops/registry-validate.py`（schema 自检）
  - `docs/ops/registry-launch.py`（消费脚本：探测 health → Official 且停机打印拉起命令（不自动执行）→ 与 production.json 交叉核对）
  - `docs/ops/registry-README.md`（说明文档）
- 审核方式：独立亲跑复现（非实现者自述采信）；只读审核，唯一写入 = 本检查点文件
- 审核门禁：§17 四项硬门禁

---

## 审核一：机器验证亲跑复现 — **PASS**

### 1.1 registry-validate.py（cwd=repo 根）
```
$ python docs/ops/registry-validate.py
  registry: ...\config\capability-registry.json
  sections: 15/15 required present | entries: 104
  warnings (24): ...（全部为非阻断：cost null note 文案 / lifecycle 历史引用）
  PASS
  EXIT=0
```
与实现者声称一致（15/15 节、104 条、24 条非阻断 warning）。

### 1.2 registry-launch.py（默认模式，不探测命令）
```
$ python docs/ops/registry-launch.py
  summary: {'official': 69, 'experimental': 32, 'deprecated': 0, 'up': 31, 'down': 7, 'skip': 66, 'official_down': 0}
  production.json cross-check: ok: True
  EXIT=0
```
- 端口/文件探测实际执行（up=31/down=7），命令探测因未加 `--probe-commands` 正确 SKIP；
- `browser-generic-profile`（experimental）DOWN 与 README 已知差异点 1（目录不存在）一致；
- catpaw 相关（experimental）DOWN 与 README 已知差异点 2（停机）一致。

### 1.3 负向测试（临时副本，5 类破坏 → validator 全部 FAIL rc=1）
用 `--registry <临时副本>` 跑，未修改正式 registry：
| 破坏 | 结果 | validator 输出样本 |
|---|---|---|
| 删除 sections.adapters | FAIL rc=1 | `missing §63 section: sections.adapters`（并暴露引用解析） |
| brains[0].status → bogus | FAIL rc=1 | `invalid status 'bogus' (must be one of [...])` |
| brains[0].adapter → adapter-nonexistent-xyz | FAIL rc=1 | `references unknown adapter 'adapter-nonexistent-xyz'` |
| 截断非法 JSON | FAIL rc=1 | `registry is not valid JSON: ...` |
| 制造重复 id | FAIL rc=1 | `role_bindings.worker references unknown id ...`（原 id 被覆盖后绑定失效） |

→ 校验真实有效（非空壳），负向路径全部正确拒绝。

### 1.4 抽查 5 条条目与磁盘/端口实测核对（全部一致）
| 条目 | registry 登记 | 磁盘/端口实测 |
|---|---|---|
| brain-chatgpt-web（official/WEB_SESSION） | entry cmd chatgpt_bridge open；health cmd chatgpt_bridge status expect "Bridge: READY" | C:/Users/17838/.local/bin/chatgpt_bridge 存在 ✓ |
| worker-local-python（official/LOCAL_RUNTIME） | health file python.exe | Python312/python.exe 存在 ✓（launch UP 一致） |
| browser-bsk（official/CDP） | entry bsk-dev.exe daemon start；health port 52900 | bsk-dev.exe 存在 + 127.0.0.1:52900 通 ✓（launch UP 一致） |
| tool-catpaw-proxy（experimental/PROXY_SERVICE） | entry start-proxy.ps1；health port 32177 note"实测 DOWN" | start-proxy.ps1 存在 + 32177 关闭 ✓（launch DOWN 一致） |
| r-trae-ralph-relay（experimental/AI_RELAY） | entry node review-relay.js watch cwd Trae-Ralph；health file review-relay.js | E:/WB/tools/Trae-Ralph/src/review-relay.js 存在 ✓（launch UP 一致） |

## 审核二：红线核查 — **PASS**

### 2.1 git worktree 改动范围
`git status -sb`（分支 v1.1-blackbox）：
- **R2 新增 4 文件确认**：`config/capability-registry.json`、`docs/ops/registry-README.md`、`docs/ops/registry-launch.py`、`docs/ops/registry-validate.py`
- 其余 worktree 内容均非 R2 引入：R1（`scripts/guard/`）、R3（`runtime/blackbox_bridge.py`、`runtime/test_blackbox_bridge_offline.py`、`docs/ops/blackbox-card.md`）、主会话（`docs/DECISION_LEDGER.md`、`docs/evidence/HANDOFF-INDEX.md`）、QA 检查点（`docs/evidence/reviews/`）——已按任务指示忽略，未发现 R2 越权改动。

### 2.2 config/production.json 未被修改
- `git diff config/production.json` → 空
- `git status --porcelain config/production.json` → 空
- registry-README §8 纪律声明与实测一致 ✓

### 2.3 凭据纪律
- registry 中 grep `"url"|"token"|"credential"|"password"|"secret"|"api_key"|"key"` → **零命中**（不复制任何凭据内容）
- `login_state` 节 6 条全部为 `CREDENTIAL_REFERENCE`，`entry.kind="path"` 只登记路径（会话注册.json、.bridge_state.json、auth-profile-v1/v2、cft-profile；catpaw-gui 只登记存在性），note 明确"只登记路径，不复制内容、不读取、不外传" ✓
- `proxy-key.dpapi` 仅登记路径，注明"DPAPI 密文，机器绑定；轮换执行主体=业主本人" ✓
- 顶层 notes："R-PROD 会话 URL 唯一来源：E:/执衡/05_资源/会话注册.json（凭据文件，只登记路径）" ✓

## 审核三：定义节证据充分性 — **PASS**

### 3.1 宪法 §63 逐项对照（原文 L1938-1962）
宪法 §63 要求机器可读能力注册表，包括 15 项：Brain/Worker/C/R/Browser/Tool/Provider/Login State/Cost/Quota/Reliability/Capabilities/Official|Experimental|Deprecated/Permissions/Adapter；目的"资源可替换"。
registry 顶层 `sections` 15 节与 §63 15 项**一一对应**（brains/workers/correctors/reviewers/browsers/tools/providers/login_state/costs/quotas/reliabilities/capabilities/lifecycle_status/permissions/adapters）+ `role_bindings` 角色映射 ✓

### 3.2 §20/§21（浏览器面/登录态/本地面）实际内容
- **§20 浏览器是通用生产执行面**（L816-844）：registry `capabilities` 节含 cap-browser-search/read/click/input/scroll/form/upload/download/login-state（9 条）✓；`browsers` 节 9 条（playwright-cdp 主/bsk 回退/cft/chrome + 各 profile）均有 entry 与 health_check ✓
- **§21 本地电脑是通用生产执行面**（L848-865）：capabilities 节含 cap-local-filesystem/shell/powershell/git/python/node/docker/api（9 条）✓
- **§59 Cost Routing**：costs（9 条）+ quotas（6 条）节为 D2 输入契约，README §5 明确衔接点；当前 cost 均为 null+"待 D2 校准"（如实登记，非编造）✓

### 3.3 Official/Experimental/Deprecated 判定规则
README §4 三档判定条件表：Official=已在 production.json/run.cmd 声明且 health_check 有实测证据；Experimental=路径存在但未在生产主链验证/当前停机/construction_only；Deprecated=已被取代或明确弃用；升/降档必须在 DECISION_LEDGER 记录。判定规则明确 ✓

## 审核四：越权与质量 — **PASS**

### 4.1 registry-launch.py 只打印不自动执行
- 源码审查：`subprocess.run` 仅 2 处（L80 直接 spawn / L92 WinError 193 shell 回退），均在 `probe_command()` 内，且命令探测受 **双重门控**：需显式 `--probe-commands` + health_check 标记 `read_only=true`；默认模式全部 SKIP（实测 3 条命令探测均 SKIP）
- 无 `os.system` / `Popen` / `start` / `CreateProcess` 调用
- launch 建议仅 `print`（"PRINTED ONLY, NOT EXECUTED"），实测 Official&DOWN=0 建议为空 ✓
- 退出码语义明确：0 正常 / 2 读文件失败 / 3 cross-check 失败 / 4 strict 且 official_down>0 ✓

### 4.2 registry-validate.py 非空壳
检查项真实且相互独立：15 节齐全、schema/schema_version、id 全局唯一、能力条目必填字段（id/name/type/status + status 枚举）、health_check 结构（port 范围/file 非空/command 列表+timeout）、cost/quota/reliability 结构、permissions、adapter 引用可解析、capability 引用 + provided_by 可解析、role_bindings 引用可解析、cost/quota/reliability 的 capability_id 链接、lifecycle 引用、metadata 各节形状。负向测试 5/5 证明其真实拦截能力 ✓

### 4.3 抽查 3 条目字段完整
brain-chatgpt-web / r-trae-ralph-relay / tool-catpaw-proxy：必填字段（id/name/role/type/status/entry/health_check）**全部齐全**，且 adapter/launch/permissions 均填写 ✓；capabilities 25 条均带 provided_by 引用 ✓

---

## 发现的缺陷
**无功能性缺陷**。3 条观察项（均非缺陷，不构成 REWORK）：
1. OBS-1（一致性）：24 条 warning 中多数为 `cost is null but note does not say '待 D2 校准'`——部分能力条目的 cost note 文案未统一含"待 D2 校准"字样（如 worker-local-python、c-ec-lite、browsers、tools、login_state 多数），属 D2 前预期内告警，建议后续统一文案以归零 warning。
2. OBS-2（如实登记，待 Controller 决策）：`browser-generic-profile` 实测目录不存在，登记为 experimental 并告警（README 已知差异点 1），符合"不编造"纪律；需 Controller 决定创建目录或修正 production.json。
3. OBS-3（低风险）：probe_command 的 shell=True 回退（WinError 193）使用拼接命令经 shell 执行，命令来源为 registry 登记的只读探测命令且受 read_only 门控，可接受；建议后续对含引号命令做审计。

## 最终判定：**APPROVED**

R2 能力注册表 + 资产接入四项交付通过 §17 四项硬门禁：
- 机器验证亲跑复现：validate PASS（15/15 节/104 条/24 warning）、launch exit 0 + cross-check ok:True、负向测试 5/5 全部 FAIL（证明校验真实）、抽查 5 条条目与磁盘/端口实测全部一致；
- 红线核查：R2 只新增 4 文件，config/production.json 零改动，registry 凭据纪律良好（零内容复制，login_state/proxy-key 仅登记路径）；
- 定义节证据充分性：宪法 §63 15 项与 registry 15 节一一对应，§20/§21 浏览器面/本地面 capabilities 有实际内容，Official/Experimental/Deprecated 判定规则在 README §4 明确；
- 越权与质量：registry-launch.py 只打印不自动执行（subprocess 双重门控）、validator 非空壳（5 类负向全拦截）、3 条目字段完整。

---

## 复审记录（2026-08-30 深夜，端口修正快速复审）— **APPROVED**

> 背景：R1 审核发现 bsk daemon 实际端口为 52800（daemon.json ws_port），R2 注册表 browser-bsk/tool-bsk-daemon/adapter-bsk 原登记 52900 继承错误假设。实现者已修正。

### 验证输出摘要
1. **validate 亲跑**：`python docs/ops/registry-validate.py` → sections 15/15、entries 104、warnings 24（同前，全部非阻断）、**PASS exit=0**
2. **抽查条目**：
   - `browser-bsk`：health_check.port=**52800**，note"端口从 bsk-home/daemon.json 动态读取为准" ✓
   - `tool-bsk-daemon`：health_check.port=**52800**（同 note）✓
   - `browser-playwright-cdp`：保持 **52900** + note 澄清"与 bsk daemon 无关，勿与 bsk 的 52800 混淆" ✓
   - `adapter-bsk`：health_check 置空 {}（adapter 为 metadata 非探活进程，合理），entry.note 注明"WS 52800（以 daemon.json ws_port 为准）" ✓
   - `brain-chatgpt-web`：launch.note 注明"以 bsk-home/daemon.json ws_port 为准；当前实测 52800" ✓
3. **daemon.json 实证**：`ws_port: 52800`、pid=16556（与实现者所述 pid 3604→16556、52900→52800 漂移一致）；netstat `127.0.0.1:52800 LISTENING 16556` ✓，52900 无监听 ✓
4. **改动范围**：git status 下 registry 4 文件仍为未跟踪（R2 交付未提交态）；mtime 确认仅 `config/capability-registry.json` 更新（00:14:39），validate/launch/README 未变（23:55-23:57）→ **本次只改 registry** ✓
5. **顺带发现（R1 回归信号）**：`scripts/guard/guard_all.cmd` mtime 00:13:53 已更新——R1 DEF-1 修复已落地：新增 `:read_bsk_port` 动态读 daemon.json ws_port（[FileShare]::ReadWrite -bor Delete 保留 EPERM 修复），fallback 52800，bsk_check 注释说明 52900 可能被 playwright-cdp 占用。实现正确（R1 正式回归另行安排）。

### 复审判定：**APPROVED**
端口修正正确、与 daemon.json/netstat 实证一致、validator 仍 PASS、仅 registry 一文件变更；顺带确认 R1 DEF-1 修复已正确落地。
