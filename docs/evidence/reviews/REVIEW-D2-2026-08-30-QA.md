# REVIEW-D2-2026-08-30-QA — D2 成本路由+熔断独立审核（QA）

- 审核者：software-qa-engineer（严过关，QA）
- 日期：2026-08-30
- 分支：v1.1-blackbox
- 审核对象（D2 交付，实现者 software-engineer-d2）：
  - `runtime/cost_router.py`（1012 行：route/budget/SAFE_HALT/simulate-rework/reset）
  - `runtime/test_cost_router_d2_offline.py`（637 行，57 用例）
  - `config/cost_policy.json`（149 行：单价表/阈值/熔断参数）
  - `config/capability-registry.json`（改动：仅 costs 节 8 条 + note 2 条）
  - `docs/ops/cost-router-README.md`（156 行）
  - `state/cost_router_state.json`（运行时工件：3 条 SAFE_HALT 记录）
- 审核方式：独立亲跑复现（Python312）；只读审核，唯一写入 = 本检查点文件；SAFE_HALT 测试用临时 state 文件（未污染正式状态文件）

---

## 审核一：机器验证亲跑复现 — **PASS**

### 1.1 测试亲跑（Python312）
```
$ ...Python312\python.exe -m unittest discover -s runtime -p "test_cost_router_d2_offline.py"
  Ran 57 tests in 0.115s — OK（57/57，与声称一致）
```

### 1.2 route 实测（临时 state，未动正式 state/cost_router_state.json）
| 用例 | 命令 | 实测输出 | 退出码 |
|---|---|---|---|
| 低风险 | `route --goal "机械读取配置文件并格式化输出" --rework-risk low` | verdict=ALLOWED, recommended=**weak**, ETC=0.05（worker 0.05×1 call） | 0 |
| 高风险 | `route --goal "high-risk production 安全发布任务需强审查" --rework-risk high` | verdict=**SAFE_HALT**（BUDGET_BREACH）, ETC=0.98, frozen=True, record SAFE_HALT-...-001 | 2 |
| 中风险 | `route --goal "分析并输出方案文档" --rework-risk mid` | recommended=hybrid, ETC=0.4865（worker 0.0695 + review 0.417） | 0 |

### 1.3 熔断状态机实测
| 用例 | 实测输出 |
|---|---|
| budget 1st 超限（--max-cost 0.3, high-risk） | verdict=**BLOCKED**, consecutive=1, suggest_safe_halt=True |
| budget 2nd 同任务 | verdict=**SAFE_HALT**（CONSECUTIVE_BREACH）, frozen=True, record SAFE_HALT-...-001 |
| simulate-rework --cycles 3 | verdict=**SAFE_HALT**（NO_PROGRESS）, count=3/3, record SAFE_HALT-...-002 |
| 冻结验证：SAFE_HALT 后同任务再 route | verdict=**FROZEN**（不自动重试） |
| 冻结验证：其他任务 | verdict=ALLOWED（recommended=weak，未被误冻） |
| reset 解冻 | detail=全部 2 个冻结任务已解冻, status=FREE, **history_count=2 保留**（证据留存） |
| 解冻后同任务再 route（预算仍超） | verdict=SAFE_HALT（重新触发，不再 FROZEN，正确） |

### 1.4 退出码约定（独立验证）
`route low=0` / `route SAFE_HALT=2` / `FROZEN=2` / `budget BLOCKED=2` / `status=0` / `reset=0` —— 与 README"0=成功/1=配置错误/2=硬停"完全一致 ✓

### 1.5 ETC 手算比对（3 例全部吻合）
| 场景 | 手算 | 脚本输出 |
|---|---|---|
| weak + low | worker 0.05×1 = **0.05** | 0.05 ✓ |
| strong + high | (0.20+0.30)×(1+0.6+0.36)=0.50×1.96 = **0.98** | 0.98 ✓ |
| hybrid + mid | (0.05+0.30)×(1+0.3+0.09)=0.35×1.39 = **0.4865**（worker 0.0695 + review 0.417） | 0.4865 / 0.0695 / 0.417 ✓ |

- E[调用次数] = Σ_{i=0}^{max_cycles-1} p^i 几何级数（p=low 0.1/mid 0.3/high 0.6；max_cycles=3）数学正确 ✓

## 审核二：红线核查 — **PASS**
- `git diff config/capability-registry.json`：**仅 costs 节 8 条**（cost_per_call null→估算值 + cost_model/source/note 补齐）+ **notes 2 条**（D2 校准说明 + 运行时消费点更新）——与实现者声称完全一致 ✓
- 凭据 grep（sk-/password/secret/会话注册内容/api_key 值）：**零命中**（cost_policy.json 的 "secret/机密" 为 goal_type_keywords 关键字，非凭据内容）✓
- 关键文件未动：`src/aicontrol/`、`config/production.json`、`runtime/runtime.py`、`runtime/adapters/` 均 git status 空 ✓
- 无 push（工作树未提交状态）✓

## 审核三：定义节证据 — **PASS**
- **§2 为什么存在执衡**（L75-105）：AI 资源错配（强 AI 贵/有额度、弱 AI 便宜适合机械劳动）——D2 三档路由 weak/hybrid/strong（弱模型便宜干活、强模型贵审查）对应 ✓
- **§59 Cost Routing**（L1836-1857）：调度综合 price/quota/latency/capability/retry cost/task difficulty；优化目标 Expected Total Cost/Expected Success——D2 ETC = Σ(阶段成本×概率权重)，几何级数体现 retry cost（REWORK 循环），goal_type+rework-risk 体现 task difficulty ✓
- **§61 Hard Fuse**（L1881-1906）：必须存在最大预算/同类失败/NO_PROGRESS 上限，触发→SAFE_HALT——D2 三条件 BUDGET_BREACH（预算上限）/CONSECUTIVE_BREACH（连续同类失败）/NO_PROGRESS 全部对应 ✓
- **§19 NO_PROGRESS**（L775-812）：计数器真实（连续 REWORK 无进展超限熔断）——`state.no_progress[goal_hash]` 真实累计 + simulate-rework 实测触发（3/3）✓（非摆设）

## 审核四：质量审查 — **PASS（含 1 条阈值意见）**
- 路由算法正确：`geometric_sum`（几何级数）、`expected_call_counts`（worker==review calls）、`tokens_scale`、阶段成本=单价×期望次数×scale、ETC=有单价阶段和（手算 3 例全吻合）✓
- SAFE_HALT 状态机完整：触发（生成结构化 record + 冻结 goal_hash + status=SAFE_HALT）→ FROZEN 拦截 → reset 解冻（可单任务/全部，history 保留）→ 解冻后重新评估 ✓
- 状态持久化：`load_state`（缺失→默认；损坏→备份 .corrupt-ts 后重建默认，不崩溃）；`save_state` 目录自动创建 ✓（write_text 非原子，但状态文件单写者场景可接受）
- CLI 退出码 0/1/2 约定与既有桥一致；异常处理（config/registry/state 错误→结构化 JSON + exit 1）✓
- 测试断言真实：57 用例、**141 assert**（抽查 test_frozen_task_blocks_retry 等断言具体，非空断言）✓
- **OBS-D2（阈值意见，非缺陷）**：默认预算阈值 0.5 下——weak 全过（0.05）；hybrid low/mid 过（0.39/0.49）、high 熔断（0.686）；**strong 全档熔断**（low 0.555/mid 0.695/high 0.98 均 >0.5）。即默认 0.5 会阻止所有 strong 路由（高价值/高风险任务），阈值**偏紧**。README 已注明"生产阈值由业主 L3 依据真实价目调整"，L2 演示用途可接受；建议在收尾文档标注此现象（high 价值任务需放宽预算或按价值动态阈值），供 L3 校准参考。

---

## 发现的缺陷
**无功能性缺陷**。1 条观察意见（OBS-D2，非缺陷，见上——默认预算 0.5 对 strong 档偏紧，L2 演示可接受，建议 L3 校准）。

## 最终判定：**APPROVED**

四项硬门禁：机器验证（57/57 绿、route weak/SAFE_HALT、budget BLOCKED→CONSECUTIVE_BREACH、simulate-rework NO_PROGRESS、冻结/reset、ETC 手算 3 例全吻合、退出码一致）、红线（registry 仅 costs+note、零凭据、关键文件未动）、定义节（§2/§59/§61/§19 全部对应）、质量（算法/状态机/持久化/退出码/异常/断言 PASS）均通过。D2 交付 APPROVED。
