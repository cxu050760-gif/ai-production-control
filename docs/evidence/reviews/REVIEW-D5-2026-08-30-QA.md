# REVIEW-D5-2026-08-30-QA — D5 自举+智能+Context Sufficiency 独立审核（QA）

- 审核者：software-qa-engineer（严过关，QA）
- 日期：2026-08-30
- 分支：v1.1-blackbox
- 审核对象（D5 交付，实现者 software-engineer-d5）：
  - 新增：`runtime/task_graph.py`（605 行，§17/§7）、`runtime/self_heal.py`（637 行，§68 转换器+fixlet）、`runtime/context_sufficiency.py`（451 行，§55 五分支）、3 测试文件（40 用例）、`docs/ops/d5-README.md`、`docs/evidence/d5/`（全链证据 7 文件）
  - 修改（自举目标）：`runtime/test_v09_attack_matrix_offline.py`（SH-001 fixlet，测量侧适配）
- 审核方式：独立亲跑复现（Python312）；只读审核，唯一写入 = 本检查点文件；转换器实测 goal 输出写 tmp（已清理）

---

## 审核一：机器验证亲跑复现 — **PASS**

### 1.1 D5 三测试 40/40
```
$ ...Python312\python.exe -m unittest discover -s runtime -p "test_*_d5_offline.py"
  Ran 40 tests — OK（task_graph 11 + self_heal 15 + context_sufficiency 14）
```

### 1.2 自举 L1 核心（双矩阵验证）
| 项 | 结果 |
|---|---|
| a. `runtime/test_v09_attack_matrix_offline.py`（修复后） | Ran 1 test **OK**（1 TestCase 内 36 组断言，36/36 绿）✓ |
| b. `runtime/test_v09_attack_matrix_on_b1_core.py`（权威矩阵） | 36/36 全 **MATCH**（V09-R1~R36 exp==obs）✓；`git diff` 该文件 **空（字节零改动）** ✓ |
| c. 转换器 `self_heal.py convert --source v09_failed_log_before_fix.txt` | valid=true、生成结构化 goal（GOAL/要什么成果/怎么算做完/约束/缺陷摘要/证据），与 `docs/evidence/d5/L1_goal_from_real_failure.goal.txt` 内容一致 ✓ |
| d. `SH001_fix.diff` 审查 | 6 hunk 全在测试文件（fixture authorization 改走外部权威路径 issue_decision_nonce→grant_authorization 与权威 runner AD-1/AD-3 语义一致；R18 期望修订为 ALLOW_DISTINCT_EFFECT + AD-5 identity proof；R21 重启语义用新 Controller 实例与 AD-4 一致）——**产品代码零改动**（无 src/aicontrol/、无 production 相关）、改动最小（+72/-10 行，含注释）✓ |

### 1.3 Task Graph（§17/§7）实测
| 命令 | 实测输出 |
|---|---|
| `build --goal-file` | valid=True、5 节点、拓扑序 [T01,T03,T02,T04,T05]、关键路径 [T03,T04,T05] 长度 3、parallel_groups [[T01],[T02,T03],[T04],[T05]]（"同时准备演示材料"被识别并行）✓ |
| `status` | valid=True、5 节点 ✓ |
| `add --parent T02 --task-id T02a` | 动态加任务 → 6 节点 ✓ |
| `brain-pick --goal` | brain_id=brain-codex-local、complexity=mid、unit_cost=0.2（规则式按复杂度选 Brain）✓ |
| 环检测 | CLI goal 拆解为串行不产生环；API 层测试 `test_t02_cycle_detected`（A→B→C→A → valid=False、cycles=[A,B,C]）证明 DAG 校验真实 ✓ |

### 1.4 §55 Context Sufficiency（五分支）
- 测试 14 用例全绿（t01 switch_local_brain / t02 switch_allowed_provider / t03 desensitize_retry / t04 human_authorization / t05 blocked_when_human_disallowed + 策略阈值/脱敏/CLI）✓
- CLI 亲跑：缺 required 信息 → decision=**SWITCH_LOCAL_BRAIN**、completeness{total:2,ok:1,missing:1,ratio:0.5}、fallback_chain=[brain-codex-local, brain-workbuddy-deepseek-v4-flash] ✓

## 审核二：红线核查 — **PASS**
- git status：**修改范围 = 仅 `runtime/test_v09_attack_matrix_offline.py`**（M）+ 新增 D5 文件（3 模块 + 3 测试 + README + evidence/d5/）；state/goals/ 为转换器输出目录 ✓
- 关键文件未动：`src/aicontrol/`、`config/production.json`、`runtime/runtime.py`、`runtime/adapters/`、`config/capability-registry.json`、`scripts/relay_autopilot.py`、`runtime/parallel_scheduler.py` 均 git status 空 ✓
- 凭据 grep：命中仅为 `context_sufficiency.py` 的**脱敏检测正则**（password/api_key/secret 等敏感词匹配规则，用于脱敏功能），非实际凭据内容 ✓
- 无 push（工作树未提交状态）✓

## 审核三：定义节证据 — **PASS**
- **§68 自举式迭代**（L2061-2082）：Stable 可开发 Candidate（创建任务/调度/修改 Candidate/运行测试/生成 Evidence）；Candidate 不得篡改 Stable 控制面——self_heal 转换器+fixlet SH-001（修复只落测试文件，不碰 TCB/冻结/权威矩阵）对应 ✓
- **§17 Task Graph**（L689-730）：系统维护正式 Task Graph（Task/依赖/READY/RUNNING/DONE/并行/Owner/输入输出）——task_graph.py 节点模型（depends_on/parallel_with/Owner/state/subtasks）+ 拓扑/关键路径/环检测对应 ✓
- **§7 Brain/Planner**（L316-340）：Brain 决定怎么走（理解 Goal/拆任务/调整 Task Graph/选择 Worker/判断调用强 AI）——build_from_goal 规则式拆解 + brain-pick 按复杂度规则式选 Brain（机器可完成部分）✓
- **§55 Context Sufficiency**（L1755-1769）：隐私策略致外部 Brain 信息不足 → 换本地 Brain/换允许 Provider/脱敏/Human Authorization/明确 BLOCKED——context_sufficiency.py 五决策分支 + 策略阈值 + 脱敏掩码 + 阻塞语义对应 ✓
- **§60 升级梯**（L1861-1877）：失败升级 9 级——README §6 实现 L0-L2（Task Graph/转换器/§55 路由），L3-L9 映射存档 ✓
- **§70 Trace/Monitoring**（L2104-2116）：回答谁做了什么/何时/哪个 Project/为何 Retry/成本——D5 输出带 `trace:{model,ai,tool,reason_retry,cost}` ✓

## 审核四：质量审查 — **PASS**
- 转换器解析鲁棒性：解析优先级 DRIFT 行 > FAILED 测试 > 异常行；测试覆盖 4 种输入（t01 drift / t02 test_failed / t03 error_text / t04 unknown）✓
- fixlet 机制：SH-001 注册（_OLD→_NEW 文本替换 + 前置条件 + 幂等校验 + py_compile 校验）、apply/verify 流程（t08 应用到 PRE_FIX 文件 / t09 幂等 / t10 dry_run / t12-t13 pipeline verify 通过/失败）✓
- Task Graph DAG：Kahn 拓扑（稳定序按 id）、环检测（未消去节点即环）、关键路径加权 DP、并行对称化、重复 id/悬空依赖报错（t01-t11）✓
- §55 五分支路由逻辑：策略驱动阈值（completeness_threshold=1.0/trust_threshold=0.5/allow_human_authorization）+ 敏感检测正则 + 脱敏掩码 + 阻塞语义（t01-t14）✓
- 测试断言真实：**124 assert**（task 40 + self_heal 43 + ctx 41），抽查 convert/fixlet 幂等/环检测/五分支断言具体 ✓

---

## 发现的缺陷
**无功能性缺陷**。

## 最终判定：**APPROVED**

四项硬门禁：机器验证（40/40 全绿；自举 L1 双矩阵 36/36 + 权威矩阵字节零改动；转换器 convert 生成结构化 goal 与证据一致；SH001 diff 最小/测量侧/产品零改动；Task Graph build/status/add/brain-pick/环检测；§55 五分支）全部通过、红线（修改范围=仅 offline 测试、7 关键文件未动、凭据零实际命中）、定义节（§68/§17/§7/§55/§60/§70 逐节对应）、质量（转换器鲁棒性/fixlet 机制/DAG/五分支/124 assert）均合格。D5 交付 APPROVED。
