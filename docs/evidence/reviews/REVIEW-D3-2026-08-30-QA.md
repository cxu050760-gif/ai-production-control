# REVIEW-D3-2026-08-30-QA — D3 Reuse/SupplyChain/Playwright 独立审核（QA）

- 审核者：software-qa-engineer（严过关，QA）
- 日期：2026-08-30
- 分支：v1.1-blackbox
- 审核对象（D3 交付）：
  - S3 实现：`scripts/reuse_gate.py`（690 行）、`scripts/supply_chain_check.py`（455 行）、`runtime/browser_adapter.py`（607 行）、`docs/ops/reuse-gate-README.md`、`docs/ops/supply-chain-README.md`、`docs/evidence/D3-BROWSER-SMOKE-20260830.md`、`docs/evidence/reuse-decisions.ndjson`
  - S4 测试：`tests/test_reuse_gate_d3_offline.py`（50）、`tests/test_supply_chain_check_d3_offline.py`（32）、`runtime/test_browser_adapter_d3_offline.py`（29）= 111 用例
- 审核方式：独立亲跑复现（Python312）；只读审核，唯一写入 = 本检查点文件；reuse_gate 门禁验证用临时 decisions 文件（正式 reuse-decisions.ndjson 未污染）；mock download 副作用文件已清理

---

## 审核一：机器验证亲跑复现 — **PASS**

### 1.1 测试亲跑（Python312）
```
$ ...Python312\python.exe -m unittest tests/test_reuse_gate_d3_offline → Ran 50, OK
$ ...Python312\python.exe -m unittest tests/test_supply_chain_check_d3_offline → Ran 32, OK
$ ...Python312\python.exe -m unittest runtime/test_browser_adapter_d3_offline → Ran 29, OK
  （合计 111/111 全绿，与声称一致）
```

### 1.2 reuse_gate 门禁强制验证（核心，临时 decisions 文件）
| 步骤 | 命令 | 实测输出 | 退出码 |
|---|---|---|---|
| check 无 require（本地命中） | `check --task "浏览器自动化下载文件能力" --search playwright download` | verdict=reuse、gate=GATE_OK、local_hits=3 | 0 |
| **check --require-decision 无记录** | 同上 + `--require-decision`（临时空 decisions） | gate=**BUILD_BLOCKED**、covering_count=0、instruction="该任务无对应 Decision 记录：先执行 record 完成留痕" | **1** |
| record 留痕 | `record --task "浏览器自动化下载文件能力" --decision reuse --evidence <路径>` | ok=True、decision=reuse、decision_id=D3-XXXXXXXX（结构化：task/decision/evidence/note/gate_check_summary/failed_approach_warning） | 0 |
| check --require-decision 有记录 | 同上 + `--require-decision` | gate=**GATE_OK**、covering_count=1、_match={'exact': True, 'shared_keywords': [...]} | 0 |

→ **门禁是真强制不是摆设**：无 Decision 记录 → BUILD_BLOCKED（exit 1）；record 后 → GATE_OK（exit 0）。decision_covers 精确匹配+关键词重叠机械规则工作正常。正式 reuse-decisions.ndjson 未污染（验证前后均 1 行）。

### 1.3 supply_chain（真实 pip-audit 扫描）
```
$ supply_chain_check.py check → ok=True, deps=1, vulns=0（litellm 1.83.0 无已知漏洞，与"litellm 应 OK"一致）
```
- 未装 pip-audit 分支 → UNKNOWN 不伪造：测试 `test_pip_audit_not_installed_all_unknown_no_fake_ok` 覆盖 ✓

### 1.4 browser_adapter
| 命令 | 实测输出 |
|---|---|
| `search --mock` | ok=True、engine=mock、result_count=5 ✓ |
| `fetch --mock` | ok=True、mock=True、title="Mock Page Title"、body_chars=18（**实际 19，见 OBS-D3b**）✓ |
| `download --mock` | ok=True、mock=True（构造 mock 文件后已清理）✓ |
| **真实 download** | `download --url https://example.com/ --dest <temp>` → ok=True、**download.bin 559 字节**（文件存在+size>0）、stdout 纯 JSON ✓（§20 download 能力真实可用） |
| PLAYWRIGHT_NOT_INSTALLED | 测试 patch `playwright_available=False` → rc=1、ok=False（覆盖）✓ |

注：真实 download 首测 example.com/robots.txt 返回 404（DOWNLOAD_FAILED 正确报错）——URL 选择问题非脚本缺陷；node TLS 警告输出到 stderr（不污染 stdout JSON）。

## 审核二：红线核查 — **PASS**
- git status：S3 7 文件 + S4 3 文件新增；`config/capability-registry.json`、`docs/DECISION_LEDGER.md`、`docs/ops/blackbox-card.md` 的 M 为主会话产物（任务指示忽略）；`state/cost_router_state.json`（D2 工件）、`tmpm8v1c53r/`（遗留）✓
- 关键文件未动：`src/aicontrol/`、`config/production.json`、`runtime/runtime.py`、`runtime/adapters/` 均 git status 空 ✓
- 凭据 grep（sk-/password/secret/token/api_key/会话注册内容/profile 长值）：命中均为 tokens 关键词变量、tempfile.mkdtemp profile 临时目录——**无实际凭据** ✓
- 无 push（工作树未提交状态）✓

## 审核三：定义节证据 — **PASS**
- **§48 External Reuse Gate**（L1567-1603）：Reuse>Adapt>Compose>Build 是**进入实质自研前的硬门禁**；任何新任务自研前必须检查 GitHub/官方/脚本/工作流——reuse_gate 四步流程（本地搜索+GitHub 搜索+判定+门禁）+ **BUILD_BLOCKED 强制**对应 ✓
- **§49 External Reuse Decision**（L1607-1642）："没有完成 Reuse Decision，不得进入 BUILD FROM SCRATCH"；进入自研前必须形成可追溯决策（searched_what/candidates/rejection_reason/build_necessity/fallback_plan）——record 结构化留痕（decision_id/task/decision/evidence/note/gate_check_summary/failed_approach_warning）对应；`--require-decision` 门禁对应"无 Decision 不得 BUILD" ✓
- **§50 Reuse 不等于 Trust**（L1645-1662）：外部方案默认不可信——decision 留痕含 evidence + failed_approach_warning（已失败路线衔接），supply_chain 依赖检查补充 ✓
- **§51 Supply Chain Gate**（L1666-1681）：按风险检查 Source/Maintainer/License/Dependencies/Network 等——supply_chain_check.py pip-audit 依赖漏洞扫描对应（L2 机械部分）✓
- **§20 浏览器是通用生产执行面**（L816-844，含 download）：browser_adapter search/fetch/download 补齐——**真实 download 实测 559 字节文件成功（§20 download ❌ 已补齐）** ✓

## 审核四：质量审查 — **PASS（2 个 S4 轻微项评估结论：均"可留"）**
- decision_covers 匹配逻辑：精确匹配（归一化相等）OR 关键词重叠（CJK 连续片段>=2 字整体一词 + ASCII 词>=3 去停用词）；机械规则合理、可解释、不猜 ✓
- supply_chain map_vulnerabilities：按包名映射漏洞（name→list），功能正确 ✓
- browser_adapter：mock/真实路径分离、错误结构化（404→DOWNLOAD_FAILED、无 playwright→PLAYWRIGHT_NOT_INSTALLED）、_sha256 校验、临时 profile 清理 ✓
- 异常处理/退出码：0=成功/1=失败/2=输入错误，与既有桥一致 ✓
- 测试断言真实：**310 assert**（reuse 130 + supply 96 + browser 84），抽查 record 追加/UNKNOWN 不伪造/PLAYWRIGHT patch 断言具体 ✓

### S4 报告 2 个轻微项评估结论
1. **map_vulnerabilities fixed_version repr**（`scripts/supply_chain_check.py` **L301** `_safe_text(v.get("fix_versions") or [], 200)` → 输出 `"['1.2.3']"` 样式）：**可留**——功能正确（漏洞映射/识别/包名关联不受影响），仅展示格式不美观；可选优化为 `", ".join(fix_versions)`，非必修。
2. **browser_adapter body_chars 18 vs 19**（`runtime/browser_adapter.py` **L344** mock fetch 硬编码 `"body_chars": 18`，实际 body_text "这是离线冒烟正文，不代表真实页面内容。"=19 字符）：**可留**——仅 mock 演示数据自相矛盾（声明的 18 与实际 19），真实路径 L386 用 `len(body)` 动态计算正确、不受影响；一行修复可选，非必修。
3. **OBS-D3（非缺陷）**：真实 download 时 node 打印 `NODE_TLS_REJECT_UNAUTHORIZED=0` 警告到 stderr（不污染 stdout JSON，真实消费方读 stdout 安全）——可选优化（抑制/重定向），非缺陷。

---

## 发现的缺陷
**无功能性缺陷**。2 个 S4 轻微项评估结论：**均可留**（见审核四）；1 条 OBS（node 警告在 stderr 不污染输出）。

## 最终判定：**APPROVED**

四项硬门禁：机器验证（111/111 全绿、**门禁强制 BUILD_BLOCKED exit 1 亲跑证实**、record 留痕、supply_chain 真实扫描、browser mock+真实 download 559 字节+PLAYWRIGHT patch）、红线（只增 S3/S4、关键文件未动、零凭据）、定义节（§48 硬门禁/§49 可追溯 Decision/§51 Supply Chain/§20 download 补齐全部对应）、质量（decision_covers/异常/退出码/310 assert 合格；S4 轻微项均可留）均通过。D3 交付 APPROVED。
