# ASSET_MAP_20260831 — 执衡资产地图 v2（标注版·AI 进门必读）

> 定位：`ASSET_INVENTORY_FINAL.md`（8-29 目录级权威盘点）的深度增补。
> 生成：2026-08-31 · 三路并行盘点汇总 · **标注规则沿用防误导交接（ZHIHENG_ANTI_MISLEADING_HANDOFF_20260828）**：
> **【实测】**= 8-31 当日工具跑出来的 ｜ **【文档】**= 引自项目自述文件，未在 8-31 复验 ｜ **【推测】**= 未验证。

---

## 0. 先说这份地图本身不可信的部分

**本文作者（施工 AI）8-31 当日犯有连环操作错误（12 次投递失败、误动浏览器、自编证据脚本），已被业主两次叫停并强制重审。** 因此：

- 本文"怎么用"一节的命令**多数经过当日实测**，但"实测通过"≠"最优用法"——最优用法以 `E:\执衡\00_先看这里\能力操作手册_20260820\06_NEW_AI_BOOTSTRAP.md` 和 `03_CAPABILITY_REGISTRY.md` 为准（本文作者 8-31 大部分时间**没读过**它们，读时已在收尾）。
- 本文**没有覆盖**：E:\AI_Projects 全部（OpenWrite_Local 7508 文件等）、E:\DeepSeekHarness、E:\ChatGPT\00_HOME、.workbuddy\traces、.codex\sessions、E:\WB\workspace 各会话目录、GitHub 远端 43 分支的内容（当日网络不可达）。覆盖面声明同防误导交接：**不超过全量三分之一**。
- 引用旧手册的条目均标【文档】——手册生成于 8-20/8-26，**业主明示：其中有些已过时、有些仍正确、有些与现实不符**，逐条核验是后续工作，不要当金科玉律。

## 1. 防骗清单（每条都踩过或险些踩过）

| 陷阱 | 真相 |
|------|------|
| 测试全绿=没问题 | **错**。8-31 前权威矩阵 36 例是恒绿假测试（main() 恒 return 0）——"绿"必须核对断言真实性【实测】 |
| R 回了 PASS=外审闭环 | **错**。8-31 纯文本通道拿到的 PASS 只有裁决行、无逐项发现、25 秒返回，被独立重审判定无效【实测】。有效外审按 V07 口径：packet+证据附件+逐项裁决 |
| `chatgpt_bridge status` READY=能投递 | **不一定**。READY 只查 daemon/扩展/upload 能力；导航还会因 bsk 自动升级横幅污染 session id 而失败（8-31 实测，已一行修复并备份）【实测】 |
| 附件上传失败=桥坏了 | **错**。桥上传能力 8-26 已校准（CALIBRATION.json），失败当日根因是无活着的浏览器窗口+bsk 版本横幅【实测+文档】 |
| 盘点说活跃副本=chat-1 | **已过时**。8-31 实际施工在 `Qoder\...\031cb4e3\b1`（v1.1-blackbox→hardening 分支）；chat-1 是 v0.9-b2 历史阶段副本；两者是同一 GitHub 远端的两个 worktree【实测：git worktree list】 |
| `ASSET_INVENTORY_FINAL` 的 "109 RUN" | **已过时**。8-31 实测 125 RUN（DONE=65，全 PASS）【实测】 |
| 手册说"冻结资产不改" | **有张力**。纪律正确，但现实会出现必须修的环境回归（如 bsk 横幅污染 wrapper）——正确做法：先报 owner、打补丁、留备份、登记例外，而不是偷改或硬扛【实测教训】 |
| 浏览器窗口 | 已有完整优化：wrapper 跨进程复用会话（.bridge_state.json）+ bsk daemon 自动停空闲会话 + browser_runtime.mjs 复用首页签。**AI 禁止自建窗口**；冷启动按 P0-A 开**默认 profile** Chrome（扩展实例 7da8483f 唯一，禁第二实例）【源码 8-31 核】 |
| 旧手册说 "R_URL 只从 05_资源\会话注册.json 取" | 【文档·未验】8-31 未打开该文件核验内容时效。取 URL 前先验 |
| 旧手册说 "通信只走 run.cmd 黑盒" | 【文档·与 8-31 实践有张力】8-31 实测 chatgpt_bridge wrapper 直连可用（status/open/send 全通）。两者关系（wrapper 是否即黑盒内部件）未核验 |

## 2. 两大资产块（副本甄别【实测 8-31】）

| 块 | 位置 | 状态 |
|----|------|------|
| 代码仓库 | worktree×2：`QoderCN\...\chat-1`（v0.9-b2@f74d48e，历史阶段）＋ `Qoder\...\031cb4e3\b1`（**当前施工**：hardening/p0-gates-20260831@cd12347） | 同一远端 `cxu050760-gif/ai-production-control`（私有）；master=V1.0 冻结（tag v1.0-engineering-close@793fa41）未动；**hardening 16 提交仅存本地（GitHub 当日不可达）=单点风险** |
| E:\WB 生产资产 | `E:\WB\tools`（Trae-Ralph 桥、bsk-file-bridge、ai-production-control 工具仓、catpaw-longcat-proxy）＋ `E:\WB\state\ai-production-control`（状态根）＋ `E:\WB\outputs` | 程序零改动；chatgpt_bridge wrapper 在 `C:\Users\17838\.local\bin`（有 .bak-20260831 备份） |

## 3. 入口速查（标注：命令当日实测情况）

| 用途 | 命令 | 标注 |
|------|------|------|
| 桥健康 | `chatgpt_bridge status` | 【实测】READY |
| 开/复用会话 | `chatgpt_bridge open <url>`（新会话=open https://chatgpt.com） | 【实测】SESSION azta（补丁后） |
| 传文件 | `chatgpt_bridge upload <file...>` | 【文档+当日 UPLOAD_FAIL 反证】环境就绪时可用；校准记录 CALIBRATION.json |
| 发消息/收回复 | `chatgpt_bridge send/receive --out` | 【文档】send 未当日实测；receive 语义读自源码 |
| 正式 RUN+三闸 | `python runtime\runtime.py start --goal --r-url` → `report [--file]` → `done` | 【实测】start/report 全通（RUN-164921）；但该 PASS 被判无效，重审要求按 §3-证据库流程重走 |
| 送审包/证据生成 | `E:\执衡\build_review_packet(_v4).py`、`create_evidence(_v4).py`、`publish_builder_ready(_v4/v5).py` | 【文档+留痕】verdict_r22_v5.txt 等可查；8-31 未实测（当日误用自编脚本，已隔离） |
| 守护 | 计划任务 `ZhihengGuard`（8-31 已禁用；恢复 `schtasks /change /tn ZhihengGuard /enable`） | 【实测】 |
| 状态体检 | `state_doctor.py`（期望 DRIFT_FREE）、`runtime health` | 【文档】8-31 未复跑 |

## 4. 真实跑通证据库（先例对照）

- RUN 总账【实测 8-31】：125 RUN，DONE=65 全 PASS（`E:\WB\state\ai-production-control\runtime-v1\runs\`）。
- 北极星 L3【实测】：RUN-20260831-120044-0e45，产物 `E:\WB\outputs\ai-production-control\polaris-l3-validate-*.txt`。
- 外审 PASS 链【文档+文件在盘】：verdict_v05_accept_evidence_4 / v06_accept_evidence_2 / v06_integrate_2 / v07_accept_evidence_2 / v07_strategic_reuse_contract_6（均 `E:\WB\temp\orchestration_20260824\`；v07-accept-1 首版 REWORK 修正后过——REWORK 是正常环节）。
- 桥防篡改校准【文档+文件在盘】：`construction-relay\web-calibration\CALIBRATION.json`（8-26）。
- relay 队列健康【实测 8-31】：inbox=0、events=24、quarantine=12（我的 12 次失败投递残骸已另移 `quarantine-agent-20260831\`）。

## 5. 8-29 以来增量（详见 git log 与隔离区 MANIFEST）

V0.9 收口（§74 签署、TCB 密封、tag 落 master）→ v1.1-blackbox 开线（56 提交）→ hardening 分支 16 提交（六闸加固+两轮内审）→ 外审闹剧与隔离（quarantine-agent-20260831）→ bridge 一行补丁（已登记例外）→ 阈值 0.5→2.0（owner 批）→ ZhihengGuard 禁用（owner 令）。

## 6. 待 owner 裁决（同前版，重申）

① 批次 A 外审按 §4 标准流程重走（packet/证据用 E:\执衡 现成脚本）；② hardening 分支推送 GitHub（消单点）；③ chatgpt_bridge 补丁追认或回滚；④ ZhihengGuard 恢复时机；⑤ 隔离区处置时机；⑥ 批次 B 开工（先补零测试欠账+补审 HEAD 两提交）。
