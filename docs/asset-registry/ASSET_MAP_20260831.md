# ASSET_MAP_20260831 — 执衡资产地图（AI 进门必读）

> 定位：`ASSET_INVENTORY_FINAL.md`（8-29 正式盘点，A-01~A-38 目录级权威清单）的 **v2 深度增补**。
> 本图回答四个问题：**用什么、怎么用、证据在哪、什么不许碰。**
> 生成：2026-08-31 · 三路并行盘点（tools / 执衡主目录+runs / git 体系）汇总。
> 纪律来源：`E:\执衡\00_先看这里\能力操作手册_20260820\06_NEW_AI_BOOTSTRAP.md`（AI 进门必读）+ `03_CAPABILITY_REGISTRY.md`。

---

## 0. AI 行为铁律（违反即事故，今日（8-31）已有人付出代价）

1. **R_URL 只从 `E:\执衡\05_资源\会话注册.json` 取**——不要问业主要 URL，不要自己造。
2. **ChatGPT 通信只走冻结桥**：统一入口 `C:\Users\17838\.local\bin\chatgpt_bridge`（status/open/upload/send/receive/close）或 Runtime 黑盒 `run.cmd`。禁止绕开自建 HTTP/附件协议。
3. **冻结资产不改**：桥（Trae-Ralph、bsk-file-bridge、chatgpt_bridge wrapper）、Runtime、审计证据、E:\执衡、E:\WB 现役目录——不清理不删除。（8-31 例外登记：chatgpt_bridge 一行补丁，见 §5-C。）
4. **动手前先读成功先例**：成功案例库见 §3。任何投递/送审，先引用一个已 PASS 的先例说明"我与它相同/差异在哪"。
5. **窗口机制已被优化过，禁止自建**：wrapper 跨进程复用会话（`.bridge_state.json`）+ bsk daemon 自动停空闲会话（daemon.log "idle session stopped"）+ browser_runtime.mjs 复用首页签关多余页。**不要自己 Start-Process 开浏览器**——冷启动时按 P0-A 启动**默认 profile Chrome**（扩展实例 7da8483f 唯一，禁止第二实例、禁止 --user-data-dir）。

## 1. 两大资产块与副本甄别

| 块 | 位置 | 今日状态 |
|----|------|---------|
| 代码仓库（git） | worktree×2：`QoderCN\...\chat-1`（v0.9-b2，历史阶段副本）＋ `Qoder\...\031cb4e3\b1`（**当前施工：v1.1-blackbox + hardening 分支**） | 同一 GitHub 远端 `cxu050760-gif/ai-production-control`（私有）的两个 worktree；master=V1.0 冻结（tag v1.0-engineering-close@793fa41）未动 |
| E:\WB 生产资产 | `E:\WB\tools`（桥管线）＋ `E:\WB\state\ai-production-control`（状态根：runtime-v1\runs、construction-relay、harness）＋ `E:\WB\outputs` | 程序零改动；状态改动仅：成本熔断解冻（owner 批）、隔离区新增 |

## 2. 怎么用（入口速查，全部实测可用）

| 用途 | 命令 | 证据 |
|------|------|------|
| 桥健康检查 | `chatgpt_bridge status` | 8-31 输出 Bridge: READY / Upload: READY |
| 开/复用会话 | `chatgpt_bridge open <url>`（新会话=open https://chatgpt.com） | 8-31 SESSION azta（修复版本横幅污染后） |
| 传文件 | `chatgpt_bridge upload <file...>`（自动轮询附件卡片） | CALIBRATION.json 8-26 校准：uploads 含 machine_evidence.json/manifest_check.py |
| 发消息 | `chatgpt_bridge send <text>`（内部 bridge_send.py 免转义） | smoke RUN-20260826-195102-04ad |
| 收回复 | `chatgpt_bridge receive --out <path>`（轮询生成完成） | 同上 |
| 正式 RUN（带三闸） | `python runtime\runtime.py start --goal G --r-url URL` → `report --run-id X --message-file F [--file 附件]` → R 裁决 → `done` | RUN-20260831-120044（L3 真实闭环 PASS）、RUN-20260831-164921 |
| 送审包生成 | `E:\执衡\build_review_packet(_v4).py`、`create_evidence(_v4).py`、`publish_builder_ready(_v4/v5).py` | verdict_r22_v5.txt 及 relay events 留痕——**禁止手写自建证据脚本** |
| 守护（桥死了拉活） | 计划任务 `ZhihengGuard`（8-31 被禁用，恢复：`schtasks /change /tn ZhihengGuard /enable`） | 截图日志：拉起 review-relay watch + bsk/chrome/state 检查 |
| 状态体检 | `python scripts\state_doctor.py`（期望 DRIFT_FREE）；`chatgpt_bridge status`；`runtime health` | 8-31 实测 |

## 3. 真实跑通证据库（先例对照用）

- **RUN 总账**：125 RUN，DONE=65 且 65/65 last_r_verdict=PASS（`E:\WB\state\ai-production-control\runtime-v1\runs\`）。
- **北极星 L3**：RUN-20260831-120044-0e45 DONE/PASS，产物 `E:\WB\outputs\ai-production-control\polaris-l3-validate-*.txt`。
- **历史外审全 PASS 链**：`E:\WB\temp\orchestration_20260824\verdict_v05_accept_evidence_4.txt`、`verdict_v06_accept_evidence_2.txt`、`verdict_v06_integrate_2.txt`、`verdict_v07_accept_evidence_2.txt`、`verdict_v07_strategic_reuse_contract_6.txt`（均为 PASS；V07-accept-evidence-1 首版 REWORK 修正后过——**REWORK 是正常环节**）。
- **外审标准流程**：packet 命名 `review_packet_<TASK>[_REWORKn].txt` 放 `E:\WB\temp\orchestration_20260824\`；verdict 命名 `verdict_<TASK>.txt`；证据用 `create_evidence_v4.py` 生成 harness 单元（勿自编校验器）。
- **校准记录**：`construction-relay\web-calibration\CALIBRATION.json`（8-26，含篡改包检测 REWORK 命中——桥的防篡改已验证）。

## 4. 8-29 盘点以来的增量（8-30~31）

1. V0.9 收口：§74 业主签署、TCB 密封、tag 落 master（v0.9-b1 线）。
2. v1.1-blackbox 开线（56 提交）：POLARIS-L3 真实闭环、端口 52800→52900 纠偏。
3. hardening/p0-gates-20260831 分支（16 提交，**仅存本地，GitHub 当前不可达=单点风险**）：P0 六闸加固+两轮内审+外审（无效 PASS 已作废，待按 §3 流程重走）。
4. 隔离区 `E:\WB\state\ai-production-control\quarantine-agent-20260831\`（12 次失败投递残骸+自编证据包 HE-BATCHA，MANIFEST.md 载明，全部可逆）。
5. chatgpt_bridge 一行补丁（session id 解析过滤 bsk 版本横幅；根因=bsk 自动升级提示污染解析；备份 .bak-20260831）——**违反"冻结不改"纪律的已登记例外，待 owner 追认或回滚**。
6. cost_policy 阈值 0.5→2.0（owner 批准，_threshold_history 留痕）；熔断 SAFE_HALT-004 已 owner reset。
7. ZhihengGuard 计划任务禁用（owner 要求；恢复命令见 §2）。

## 5. 待 owner 裁决清单

| # | 事项 | 建议 |
|---|------|------|
| 1 | 批次 A 外审重走（按 §3 标准流程） | 建议重走；packet/证据用现成脚本生成 |
| 2 | hardening 分支推送 GitHub | 网络恢复后推送（消单点风险） |
| 3 | chatgpt_bridge 补丁追认或回滚 | 建议追认（根因明确、有备份） |
| 4 | ZhihengGuard 恢复时机 | 需要投递/守护时再启用 |
| 5 | 隔离区处置 | 保留至外审重走完成后清理 |
| 6 | 批次 B（GATE-5 承诺面收编+GATE-6 两环转真） | 外审闭环后开工；先补零测试欠账 |
